import 'package:flutter/material.dart';

class VentaPlantasPage extends StatelessWidget {
  static final String routeName = 'ventaplantas';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Venta de plantas'),
      ),
    );
  }
}
